# 🚀 Настройка сервера для автоматического деплоя через Git

## Что было сделано

✅ Обновлен скрипт `deploy.sh` - теперь автоматически обновляет код из Git перед сборкой  
✅ Добавлена конфигурация `nginx.conf` с правильными заголовками кеширования  
✅ Созданы инструкции по деплою  
✅ Все изменения отправлены в репозиторий: `https://github.com/MaRiNaChursina/Kids_shop.git`

## 📋 Настройка на сервере (Ubuntu 24.04)

### Шаг 1: Подключение к серверу

```bash
ssh user@91.229.9.244
```

### Шаг 2: Клонирование/обновление репозитория

Если проект еще не на сервере:
```bash
cd ~
git clone https://github.com/MaRiNaChursina/Kids_shop.git kids_shop
cd kids_shop
```

Если проект уже есть:
```bash
cd /path/to/kids_shop
git pull origin main
```

### Шаг 3: Установка зависимостей (если нужно)

```bash
# Установка Node.js (если не установлен)
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установка зависимостей frontend
cd frontend
npm install
cd ..
```

### Шаг 4: Настройка nginx

```bash
# Копирование конфигурации
sudo cp nginx.conf /etc/nginx/sites-available/kids_shop

# Редактирование пути к проекту
sudo nano /etc/nginx/sites-available/kids_shop
```

**ВАЖНО**: Измените строку:
```
root /path/to/kids_shop/frontend/build;
```

На реальный путь, например:
```
root /home/username/kids_shop/frontend/build;
```

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/kids_shop /etc/nginx/sites-enabled/

# Удаление дефолтной конфигурации (если нужно)
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации
sudo nginx -t

# Перезагрузка nginx
sudo systemctl reload nginx
```

### Шаг 5: Настройка прав доступа

```bash
# Дать права на выполнение скрипта деплоя
chmod +x deploy.sh

# Убедиться, что у пользователя есть права на директорию
sudo chown -R $USER:$USER /path/to/kids_shop
```

## 🎯 Использование

### Первая сборка и деплой

```bash
cd /path/to/kids_shop
./deploy.sh
```

### Обновление после изменений в Git

1. **На локальной машине** (Windows):
```bash
cd "F:\бизнес\Свои курсы и интенсивы\Astracoin.ru\kids_shop"
git add .
git commit -m "Описание изменений"
git push origin main
```

2. **На сервере** (Linux):
```bash
cd /path/to/kids_shop
./deploy.sh
```

Скрипт автоматически:
- ✅ Обновит код из Git (`git pull`)
- ✅ Установит зависимости (если нужно)
- ✅ Пересоберет frontend
- ✅ Перезагрузит nginx

## 🔍 Проверка

После деплоя проверьте:

```bash
# Проверка заголовков кеширования
curl -I http://91.229.9.244/index.html

# Должны быть заголовки:
# Cache-Control: no-cache, no-store, must-revalidate
```

В браузере:
- Очистите кеш (`Ctrl + Shift + Delete`)
- Или используйте режим инкогнито
- Или жесткая перезагрузка (`Ctrl + F5`)

## 📝 Важные моменты

1. **Git репозиторий**: `https://github.com/MaRiNaChursina/Kids_shop.git`
2. **Ветка**: `main`
3. **Путь к проекту на сервере**: замените `/path/to/kids_shop` на реальный путь
4. **Nginx конфигурация**: не забудьте изменить путь в `nginx.conf`

## 🆘 Решение проблем

### Проблема: Git требует аутентификацию

Если при `git pull` требуется пароль, настройте SSH ключи или используйте токен:

```bash
# Настройка SSH ключа (рекомендуется)
ssh-keygen -t ed25519 -C "your.email@example.com"
cat ~/.ssh/id_ed25519.pub
# Добавьте этот ключ в GitHub: Settings → SSH and GPG keys

# Или используйте токен доступа
git remote set-url origin https://YOUR_TOKEN@github.com/MaRiNaChursina/Kids_shop.git
```

### Проблема: Конфликты при git pull

Если есть локальные изменения на сервере:

```bash
# Сохранить изменения
git stash

# Обновить из репозитория
git pull origin main

# Применить сохраненные изменения (если нужно)
git stash pop
```

### Проблема: Недостаточно памяти при сборке

Для сервера с 1 ГБ RAM:

```bash
# Увеличить лимит памяти для Node.js
export NODE_OPTIONS="--max-old-space-size=512"
npm run build
```

## 📚 Дополнительная информация

- Подробная инструкция: `DEPLOY.md`
- Быстрое решение: `QUICK_FIX.md`

