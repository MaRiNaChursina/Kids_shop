# 🚀 Быстрое решение проблемы с обновлением Frontend

## Проблема
Frontend не обновляется на сервере cloud.reg.ru после внесения изменений.

## ⚡ Быстрое решение (5 минут)

### Шаг 1: Подключитесь к серверу
```bash
ssh user@91.229.9.244
```

### Шаг 2: Перейдите в проект и запустите деплой
```bash
cd /path/to/kids_shop  # Замените на ваш реальный путь
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- ✅ Обновит код из Git репозитория (git pull)
- ✅ Пересоберет frontend
- ✅ Обновит файлы в папке build
- ✅ Перезагрузит nginx

### Шаг 3: Очистите кеш браузера
- Нажмите `Ctrl + Shift + Delete`
- Или используйте режим инкогнито (`Ctrl + Shift + N`)
- Или жесткая перезагрузка: `Ctrl + F5`

## 🔧 Если скрипт не работает

### Ручная пересборка:
```bash
cd /path/to/kids_shop/frontend
rm -rf build
npm run build
sudo systemctl reload nginx
```

## ⚙️ Если nginx не настроен

1. Скопируйте конфигурацию:
```bash
sudo cp nginx.conf /etc/nginx/sites-available/kids_shop
sudo ln -s /etc/nginx/sites-available/kids_shop /etc/nginx/sites-enabled/
```

2. Отредактируйте путь в конфигурации:
```bash
sudo nano /etc/nginx/sites-available/kids_shop
```
Измените `root /path/to/kids_shop/frontend/build;` на реальный путь.

3. Проверьте и перезагрузите:
```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 📋 Проверка

После деплоя проверьте:
```bash
# Проверка заголовков кеширования
curl -I http://91.229.9.244/index.html

# Должны быть заголовки:
# Cache-Control: no-cache, no-store, must-revalidate
```

## 💡 Важно

- **index.html** НЕ кешируется - всегда свежий
- **JS/CSS файлы** кешируются надолго (у них хеши в именах)
- При изменении кода имена файлов меняются автоматически

## 📚 Подробная инструкция

См. файл `DEPLOY.md` для детальной информации.

